import React from 'react';

interface Props {

}

const Header = (_: Props) => {

    return (
        <div style={{ height: '40px', background: '#f3f7ec' }}>

        </div>
    );
};

export default Header;
